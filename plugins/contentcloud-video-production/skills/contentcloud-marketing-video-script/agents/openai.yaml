interface:
  display_name: "ContentCloud 营销视频剧本"
  short_description: "生成带可信引用、批次实验和镜头约束的AI视频剧本"
  default_prompt: "Use $contentcloud-marketing-video-script to create governed content from the selected V3 ContentBatch."

policy:
  allow_implicit_invocation: true
