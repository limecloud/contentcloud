import { AlertCircle, ArrowRight, BookOpen, Check, CheckCircle2, ChevronRight, FileCheck2, FileInput, ListTodo, PackageCheck, Plus, Search, ShieldAlert, ShieldCheck, X, XCircle } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Button, Empty, Field, IconButton, Modal } from '../../components/ui';
import type { EvidenceSpan, KnowledgeObject, KnowledgePack, KnowledgeQueryResult, KnowledgeSnapshot, Source, SourceRevision } from '../../types';
import { evidenceLocatorLabel, knowledgeBlockReasonLabel, knowledgeLayerLabel, knowledgeNextActionLabel, knowledgeObjectTypeLabel, knowledgeObjectTypeValue, knowledgePackPurposeLabel, sourceTypeLabel, statusLabel } from '../../uiLabels';
import { knowledgeApi, type GovernedKnowledgeObject } from './knowledgeApi';
import '../../styles/workSurface.css';

export interface KnowledgeProject {id:string;brand_name:string;product_name:string}
type KnowledgeTab='overview'|'objects'|'review'|'sources'|'packs'|'query';
type KnowledgeDecision='approve'|'reject';

const knowledgeStatusLabels:Record<string,string>={candidate:'候选',needs_review:'待审核',pending:'待处理',verified:'已核验',approved:'已批准',valid:'有效',active:'生效中',blocked:'已阻断',conflicted:'有冲突',prohibited:'已禁止',expired:'已过期',rejected:'已拒绝',superseded:'已替代',revoked:'已撤销',open:'待解决',source_missing:'缺少来源',collecting:'补料中',resolved:'已解决',waived:'已豁免',accepted_risk:'已接受风险',published:'已发布',draft:'草稿',retired:'已停用'};

export function knowledgeObjectAllows(value:{allowed_actions?:readonly string[]},action:KnowledgeDecision){return value.allowed_actions?.includes(action)??false}
function knowledgeObjectNeedsAttention(value:GovernedKnowledgeObject){return Boolean(value.allowed_actions?.length)||['evidence_required','requires_resolution'].includes(value.governance_state||'')}
function latestKnowledgeObjects<T extends KnowledgeObject>(values:T[]){const latest=new Map<string,T>();for(const value of values){const previous=latest.get(value.id);if(!previous||value.version>previous.version)latest.set(value.id,value)}return [...latest.values()].sort((left,right)=>left.title.localeCompare(right.title,'zh-CN'))}

export function GovernedKnowledgePage({projects,projectID,onProject}:{projects:KnowledgeProject[];projectID?:string;onProject?:(projectID:string)=>void}){
  const project=projects.find(item=>item.id===projectID)||projects[0];
  const navigate=useNavigate();
  const [tab,setTab]=useState<KnowledgeTab>('overview');
  const [objects,setObjects]=useState<GovernedKnowledgeObject[]>([]);
  const [packs,setPacks]=useState<KnowledgePack[]>([]);
  const [sources,setSources]=useState<Source[]>([]);
  const [selected,setSelected]=useState<GovernedKnowledgeObject>();
  const [query,setQuery]=useState('');
  const [notice,setNotice]=useState('');
  const [loading,setLoading]=useState(true);
  const [error,setError]=useState('');
  const [actionError,setActionError]=useState('');

  useEffect(()=>{
    if(!project){setLoading(false);return}
    let active=true;
    setLoading(true);setError('');setSelected(undefined);
    Promise.all([knowledgeApi.objects(project.id),knowledgeApi.packs(project.id),knowledgeApi.sources(project.id)])
      .then(([nextObjects,nextPacks,nextSources])=>{if(active){setObjects(latestKnowledgeObjects(nextObjects));setPacks(nextPacks);setSources(nextSources)}})
      .catch(value=>{if(active)setError(value instanceof Error?value.message:'知识库加载失败')})
      .finally(()=>{if(active)setLoading(false)});
    return()=>{active=false};
  },[project?.id]);

  const filtered=objects.filter(item=>tab!=='review'||knowledgeObjectNeedsAttention(item)).filter(item=>query.trim()===''||`${item.title}${item.statement}${item.object_type}${item.layer}`.toLowerCase().includes(query.trim().toLowerCase()));
  const transition=async(decision:KnowledgeDecision,reason:string)=>{
    if(!selected||!project||!knowledgeObjectAllows(selected,decision))return;
    try{
      const result=await knowledgeApi.transition(selected.id,{expected_version:selected.version,expected_digest:selected.digest,decision,reason});
      setObjects(current=>latestKnowledgeObjects<GovernedKnowledgeObject>([...current,result.object]));
      setSelected(undefined);setNotice(decision==='approve'?'知识对象已生成新的已治理版本，历史版本保持不变。':'候选版本已拒绝，决定和历史版本已保留。');setActionError('');
      try{setObjects(latestKnowledgeObjects(await knowledgeApi.objects(project.id)))}catch{setActionError('知识决定已保存，但列表动作状态刷新失败；重新打开知识库后会从服务端恢复。')}
    }catch(value){setActionError(value instanceof Error?value.message:'知识决策失败')}
  };

  if(!project)return <div className="studio-view workos-page"><PageHeader eyebrow="客户创作台 / 知识库" title="知识库" description="知识对象必须归属一个真实项目。"/><Empty title="还没有项目" detail="项目创建后，知识对象、来源和快照会在这里统一管理。"/></div>;
  if(loading)return <div className="studio-view workos-page knowledge-page"><PageHeader eyebrow={`项目 / ${project.brand_name} / 知识库`} title="知识库" description="正在读取服务端知识对象和知识包。"/><div className="workos-loading">正在读取真实知识库…</div></div>;
  return <div className="studio-view workos-page knowledge-page">
    <PageHeader eyebrow={`项目 / ${project.brand_name} / 知识库`} title="知识库" description="在同一页面管理知识对象、来源、证据和可发布快照。" actions={<>{projects.length>1&&onProject&&<label className="knowledge-project-select"><span>当前项目</span><select value={project.id} onChange={event=>onProject(event.target.value)}>{projects.map(item=><option key={item.id} value={item.id}>{item.brand_name} · {item.product_name}</option>)}</select></label>}<Button variant="secondary" onClick={()=>setTab('sources')}><FileInput size={15}/>登记来源</Button><Button onClick={()=>navigate(`/studio/tasks/new?project=${encodeURIComponent(project.id)}`)}><Plus size={15}/>创建补料任务</Button></>}/>
    {error&&<div className="workos-notice is-error"><AlertCircle size={16}/><span>{error}</span></div>}
    {actionError&&<div className="workos-notice is-error"><AlertCircle size={16}/><span>{actionError}</span><IconButton label="关闭错误" onClick={()=>setActionError('')}><X size={15}/></IconButton></div>}
    <section className="knowledge-summary"><KnowledgeMetric label="对象总数" value={String(objects.length)} detail="当前项目"/><KnowledgeMetric label="待处理" value={String(objects.filter(knowledgeObjectNeedsAttention).length)} detail="当前账号可以处理" tone="warning"/><KnowledgeMetric label="冲突" value={String(objects.filter(item=>item.status==='conflicted'||item.object_type==='ConflictRecord').length)} detail="需要解决后再使用" tone="danger"/><KnowledgeMetric label="知识缺口" value={String(objects.filter(item=>item.object_type==='KnowledgeGap').length)} detail="可创建补料任务" tone="info"/><KnowledgeMetric label="已发布知识包" value={String(packs.filter(pack=>pack.status==='published').length)} detail={`${sources.length} 个来源`} tone="success"/></section>
    {notice&&<div className="workos-notice is-success"><CheckCircle2 size={16}/><span>{notice}</span><IconButton label="关闭提示" onClick={()=>setNotice('')}><X size={15}/></IconButton></div>}
    <div className="knowledge-tabs">{([['overview','概览'],['objects','知识对象'],['review','待审核与冲突'],['sources','来源与证据'],['packs','知识包与快照'],['query','知识查询']] as const).map(([id,label])=><button key={id} className={tab===id?'is-active':''} onClick={()=>setTab(id)}>{label}</button>)}</div>
    {tab==='overview'?<KnowledgeOverview objects={objects} onTab={setTab} onSelect={setSelected}/>:tab==='sources'?<KnowledgeSources projectID={project.id} sources={sources} onCreated={async()=>{try{setSources(await knowledgeApi.sources(project.id));setNotice('来源已登记，等待本地解析并上报证据。');setActionError('')}catch(value){setActionError(value instanceof Error?value.message:'来源列表刷新失败')}}}/>:tab==='packs'?<KnowledgePacks projectID={project.id} packs={packs} objects={objects} onChanged={async()=>setPacks(await knowledgeApi.packs(project.id))} onError={setActionError}/>:tab==='query'?<KnowledgeQuery projectID={project.id} packs={packs} query={query} setQuery={setQuery} onNotice={setNotice} onError={setActionError}/>:<KnowledgeObjectTable tab={tab} query={query} onQuery={setQuery} objects={filtered} onSelect={setSelected}/>}
    {selected&&<KnowledgeDrawer value={selected} onClose={()=>setSelected(undefined)} onDecision={transition} onTask={()=>{setSelected(undefined);navigate(`/studio/tasks/new?project=${encodeURIComponent(project.id)}`)}}/>}
  </div>;
}

function KnowledgeOverview({objects,onTab,onSelect}:{objects:GovernedKnowledgeObject[];onTab:(tab:KnowledgeTab)=>void;onSelect:(object:GovernedKnowledgeObject)=>void}){
  const layers=[['identity','品牌身份'],['product','产品信息'],['market','市场洞察'],['expression','内容表达'],['operations','业务运营'],['content_engine','内容方法'],['compliance','合规要求']];
  const stateFor=(layer:string)=>{const scoped=objects.filter(item=>item.layer===layer);if(scoped.length===0)return'未建立';if(scoped.some(item=>item.status==='conflicted'||item.object_type==='ConflictRecord'))return'有冲突';if(scoped.some(knowledgeObjectNeedsAttention))return'待处理';return'已覆盖'};
  const reviewObjects=objects.filter(knowledgeObjectNeedsAttention).slice(0,4);
  return <div className="knowledge-overview-grid"><section className="workos-section"><SectionHeader kicker="七层知识" title="知识覆盖情况"/><div className="coverage-list">{layers.map(([layer,label])=>{const state=stateFor(layer);return <button key={layer} onClick={()=>onTab('objects')}><span className="coverage-bar"><i className={state==='已覆盖'?'is-full':state==='有冲突'?'is-danger':'is-half'}/></span><strong>{label}</strong><small>{state}</small><ChevronRight size={14}/></button>})}</div></section><section className="workos-section"><SectionHeader kicker="待处理" title="需要你处理的对象" action={<button className="text-action" onClick={()=>onTab('review')}>查看待处理对象 <ArrowRight size={14}/></button>}/><div className="knowledge-review-list">{reviewObjects.length===0?<Empty title="没有待处理对象" detail="当前账号没有可处理的知识对象。"/>:reviewObjects.map(item=><button key={`${item.id}:${item.version}`} onClick={()=>onSelect(item)}><span className="object-mark is-review"><BookOpen size={15}/></span><span><strong>{item.title}</strong><small>{knowledgeStatusLabels[item.status]||statusLabel(item.status)} · {item.impact||'需要确认来源'}</small></span><ChevronRight size={14}/></button>)}</div></section><section className="workos-section knowledge-query-card"><SectionHeader kicker="知识查询" title="查询前先选择范围"/><p>查询结果分为可用、已阻断和知识缺口三类。查询不会修改知识对象，也不会把已阻断内容当作可用事实。</p><button className="text-action" onClick={()=>onTab('query')}>运行一次查询 <ArrowRight size={14}/></button></section></div>;
}

function KnowledgeObjectTable({tab,query,onQuery,objects,onSelect}:{tab:KnowledgeTab;query:string;onQuery:(value:string)=>void;objects:GovernedKnowledgeObject[];onSelect:(value:GovernedKnowledgeObject)=>void}){return <section className="workos-section knowledge-object-section"><div className="knowledge-toolbar"><label className="search-field"><Search size={16}/><input value={query} onChange={event=>onQuery(event.target.value)} placeholder="搜索对象、陈述或类型" aria-label="搜索知识对象"/></label><span>{objects.length} 个对象</span></div><div className="knowledge-object-table"><div className="knowledge-table-head"><span>对象</span><span>层级 / 类型</span><span>状态</span><span>证据</span><span>影响</span><span/></div>{objects.length===0?<Empty title={tab==='review'?'没有待处理对象':'还没有知识对象'} detail={tab==='review'?'当前账号没有需要处理的知识对象。':'本地客户端提交候选并完成来源绑定后，对象会显示在这里。'}/>:objects.map(item=><button className="knowledge-object-row" key={`${item.id}:${item.version}`} onClick={()=>onSelect(item)}><div><strong>{item.title}</strong><small><code>{item.id}</code> · v{item.version}</small></div><div><span className="layer-label">{knowledgeLayerLabel(item.layer)}</span><small>{knowledgeObjectTypeLabel(item.object_type)}</small></div><StatusText value={item.status}/><div className="evidence-count"><FileCheck2 size={14}/>{item.evidence_refs.length?`${item.evidence_refs.length} 条引用`:'缺少证据'}</div><small>{item.impact||'未标记'}</small><ChevronRight size={15}/></button>)}</div></section>}

function KnowledgeSources({projectID,sources,onCreated}:{projectID:string;sources:Source[];onCreated:()=>Promise<void>}){
  const [name,setName]=useState('');const [file,setFile]=useState<File>();const [busy,setBusy]=useState(false);const [notice,setNotice]=useState('');const [selectedID,setSelectedID]=useState('');const [revisionFile,setRevisionFile]=useState<File>();const [revisions,setRevisions]=useState<SourceRevision[]>([]);const [evidence,setEvidence]=useState<EvidenceSpan[]>([]);const selected=sources.find(source=>source.id===selectedID);
  useEffect(()=>{if(!selected){setRevisions([]);setEvidence([]);return}let active=true;knowledgeApi.sourceRevisions(selected.id).then(next=>{if(!active)return[];setRevisions(next);return next[0]?knowledgeApi.evidence(next[0].id):[]}).then(next=>{if(active)setEvidence(next)}).catch(()=>{if(active){setRevisions([]);setEvidence([])}});return()=>{active=false}},[selected?.id]);
  const create=async()=>{if(!name.trim()||!file){setNotice('请填写来源名称并选择文件。');return}setBusy(true);setNotice('');try{const form=new FormData();form.append('file',file);form.append('name',name.trim());form.append('source_type','local_import');await knowledgeApi.uploadSource(projectID,form);setName('');setFile(undefined);await onCreated();setNotice('来源已上传，等待本地解析器生成证据。')}catch(value){setNotice(value instanceof Error?value.message:'来源上传失败')}finally{setBusy(false)}};
  const revise=async()=>{if(!selected||!revisionFile){setNotice('请选择要追加到当前来源的文件。');return}setBusy(true);setNotice('');try{const form=new FormData();form.append('file',revisionFile);await knowledgeApi.uploadSourceRevision(selected.id,form);setRevisionFile(undefined);setNotice('来源新版本已上传，旧证据会保留并等待重新解析。');setRevisions(await knowledgeApi.sourceRevisions(selected.id))}catch(value){setNotice(value instanceof Error?value.message:'来源新版本上传失败')}finally{setBusy(false)}};
  return <div className="knowledge-subview-grid"><section className="workos-section knowledge-subview"><SectionHeader kicker="来源与证据" title="登记来源并定位证据" action={<span className="section-count">{sources.length} 个来源</span>}/><div className="source-create-form"><Field label="来源名称"><input value={name} onChange={event=>setName(event.target.value)} placeholder="例如：品牌创作简报 / 本地会话摘录"/></Field><Field label="本地文件"><input type="file" accept=".pdf,.docx,.xlsx,.pptx,.png,.jpg,.jpeg,.txt" onChange={event=>setFile(event.target.files?.[0])}/></Field><Button onClick={create} disabled={busy||!name.trim()||!file}><Plus size={15}/>{busy?'上传中…':'上传来源'}</Button></div>{notice&&<div className="workos-notice is-info">{notice}</div>}<div className="source-list">{sources.length===0?<Empty title="暂无已登记来源" detail="上传本地文件后，系统会保存内容摘要，再由解析器生成证据。"/>:sources.map(source=><button className={`source-row ${source.id===selectedID?'is-selected':''}`} key={source.id} onClick={()=>setSelectedID(source.id)}><span className="object-mark is-source"><FileInput size={15}/></span><span><strong>{source.name}</strong><small>{sourceTypeLabel(source.source_type)} · {source.revision_count} 个版本 · {statusLabel(source.status)}</small></span><ChevronRight size={15}/></button>)}</div></section><section className="workos-section knowledge-subview"><SectionHeader kicker="证据" title={selected?selected.name:'选择一个来源'}/>{!selected?<Empty title="还没有选择来源" detail="选择左侧来源，查看文件版本、解析状态和证据数量。"/>:<><div className="source-detail-facts"><span>最新版本 <b>{revisions[0]?.file_name||'未上传'}</b></span><span>解析状态 <b>{statusLabel(revisions[0]?.processing_status||selected.status)}</b></span><span>证据 <b>{evidence.length}</b></span></div><div className="source-revision-upload"><input type="file" accept=".pdf,.docx,.xlsx,.pptx,.png,.jpg,.jpeg,.txt" onChange={event=>setRevisionFile(event.target.files?.[0])}/><Button variant="secondary" onClick={revise} disabled={busy||!revisionFile}><FileInput size={15}/>上传新版本</Button></div><div className="evidence-list">{evidence.length===0?<Empty title="尚无证据" detail="本地解析完成后，证据会保留原文位置、引用文本和审核状态。"/>:evidence.map(item=><div className="evidence-row" key={item.id}><span className="object-mark is-source"><FileCheck2 size={15}/></span><div><strong>{item.quote_text}</strong><small>{evidenceLocatorLabel(item.locator_kind)} · {statusLabel(item.review_status)}</small></div><StatusText value={item.review_status}/><ChevronRight size={14}/></div>)}</div></>}</section></div>;
}

function KnowledgePacks({projectID,packs,objects,onChanged,onError}:{projectID:string;packs:KnowledgePack[];objects:KnowledgeObject[];onChanged:()=>Promise<void>;onError:(value:string)=>void}){
  const [name,setName]=useState('内容生产知识包');const [purpose,setPurpose]=useState('内容生产');const [busy,setBusy]=useState(false);const [snapshots,setSnapshots]=useState<Record<string,KnowledgeSnapshot[]>>({});const eligible=objects.filter(object=>['approved','verified','valid','active'].includes(object.status));
  useEffect(()=>{let active=true;setSnapshots({});packs.filter(pack=>pack.status==='published').forEach(pack=>knowledgeApi.snapshots(projectID,pack.id).then(next=>{if(active)setSnapshots(current=>({...current,[pack.id]:next}))}).catch(value=>{if(active)onError(value instanceof Error?value.message:'知识快照加载失败')}));return()=>{active=false}},[packs,projectID]);
  const create=async()=>{if(!name.trim()||eligible.length===0)return;setBusy(true);try{await knowledgeApi.createPack(projectID,{name:name.trim(),purpose,object_refs:eligible.map(object=>({object_id:object.id,version:object.version})),query_policy:{eligible_statuses:['approved','verified','valid','active'],allowed_object_types:[],require_evidence:true,block_on_conflict:true,block_on_rights_failure:true}});await onChanged();onError('')}catch(value){onError(value instanceof Error?value.message:'知识包创建失败')}finally{setBusy(false)}};
  const publish=async(packID:string)=>{setBusy(true);try{await knowledgeApi.publishPack(packID);await onChanged();onError('')}catch(value){onError(value instanceof Error?value.message:'知识包发布失败')}finally{setBusy(false)}};
  return <div className="knowledge-subview-grid"><section className="workos-section"><SectionHeader kicker="知识包" title="按业务用途固定知识范围" action={<span className="section-count">{packs.length} 个包</span>}/><div className="pack-create-form"><Field label="名称"><input value={name} onChange={event=>setName(event.target.value)}/></Field><Field label="用途"><input value={purpose} onChange={event=>setPurpose(event.target.value)}/></Field><span className="pack-selection-note">将固定 {eligible.length} 个已核验对象版本</span><Button onClick={create} disabled={busy||eligible.length===0}><Plus size={15}/>创建草稿</Button></div><div className="pack-list">{packs.length===0?<Empty title="还没有知识包" detail="先完成知识对象审核，再按业务用途创建可发布的知识包。"/>:packs.map(pack=><article key={pack.id}><div><strong>{pack.name}</strong><span>{knowledgePackPurposeLabel(pack.purpose)} · {pack.object_refs.length} 个对象 · v{pack.version}</span></div><StatusText value={pack.status}/>{pack.status==='draft'&&<Button variant="secondary" disabled={busy} onClick={()=>publish(pack.id)}><PackageCheck size={14}/>发布</Button>}<ChevronRight size={15}/></article>)}</div></section><section className="workos-section"><SectionHeader kicker="固定快照" title="知识包发布后自动生成"/><div className="snapshot-list">{Object.keys(snapshots).length===0?<Empty title="还没有知识快照" detail="发布知识包后，系统会生成不可变快照，供任务明确绑定。"/>:Object.entries(snapshots).flatMap(([packID,values])=>values.map(snapshot=><article className="snapshot-row" key={snapshot.id}><div><strong>{snapshot.id.slice(0,14)}…</strong><span>知识包 {packID.slice(0,14)}… · {snapshot.objects.length} 个对象</span></div><code>{snapshot.digest.slice(0,18)}…</code></article>))}</div></section></div>;
}

function KnowledgeQuery({projectID,packs,query,setQuery,onNotice,onError}:{projectID:string;packs:KnowledgePack[];query:string;setQuery:(value:string)=>void;onNotice:(value:string)=>void;onError:(value:string)=>void}){
  const publishedPacks=useMemo(()=>packs.filter(pack=>pack.status==='published'),[packs]);const [packID,setPackID]=useState('');const [channel,setChannel]=useState('short_video');const [result,setResult]=useState<KnowledgeQueryResult>();const [busy,setBusy]=useState(false);
  useEffect(()=>{if(publishedPacks.length===0){setPackID('');setResult(undefined);return}if(!publishedPacks.some(pack=>pack.id===packID))setPackID(publishedPacks[0].id)},[packID,publishedPacks]);
  const run=async()=>{if(!packID)return;const objectTypes=query.split(/[\n,，]/).map(knowledgeObjectTypeValue).filter(Boolean);setBusy(true);onError('');try{const value=await knowledgeApi.query({project_id:projectID,pack_id:packID,channel,object_types:objectTypes,at:new Date().toISOString()});setResult(value);onNotice(`查询完成：${value.eligible.length} 个可用对象，${value.blocked.length} 个已阻断对象，${value.gaps.length} 个知识缺口。`)}catch(value){onError(value instanceof Error?value.message:'知识查询失败')}finally{setBusy(false)}};
  if(publishedPacks.length===0)return <div className="query-empty"><BookOpen size={22}/><strong>先发布一个知识包</strong><span>确定性查询只读取不可变快照，不直接读取候选对象。</span></div>;
  return <div className="knowledge-query-layout"><section className="workos-section"><SectionHeader kicker="查询范围" title="从已发布知识快照中查询"/><Field label="知识包"><select value={packID} onChange={event=>{setPackID(event.target.value);setResult(undefined)}}>{publishedPacks.map(pack=><option key={pack.id} value={pack.id}>{pack.name} · v{pack.version}</option>)}</select></Field><Field label="渠道"><select value={channel} onChange={event=>setChannel(event.target.value)}><option value="short_video">短视频</option><option value="wechat_article">公众号文章</option><option value="">不限制渠道</option></select></Field><Field label="对象类型（可选）"><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="例如：营销主张、品牌规则"/><small className="field-hint">多个类型用逗号分隔；留空表示查询整个快照。</small></Field><Button onClick={run} disabled={busy||!packID}><Search size={15}/>{busy?'查询中…':'运行知识查询'}</Button></section><section className="workos-section query-result"><SectionHeader kicker="查询结果" title={result?'知识可用性':'尚未运行查询'} action={result?<span className="query-digest">{result.query_digest}</span>:undefined}/>{!result?<div className="query-empty"><Search size={22}/><strong>选择范围后运行查询</strong><span>结果只来自已发布快照，查询本身不会改变任何知识对象。</span></div>:<><QueryResultRow label="可用" value={String(result.eligible.length)} detail="满足状态、证据、冲突和权利约束" tone="is-success"/><QueryResultRow label="已阻断" value={String(result.blocked.length)} detail="存在阻断原因，不能直接作为内容事实" tone="is-danger"/><QueryResultRow label="知识缺口" value={String(result.gaps.length)} detail="需要创建补料任务或补充知识" tone="is-warning"/>{result.blocked.length>0&&<div className="query-detail-list">{result.blocked.slice(0,5).map(item=><div key={item.object_id}><strong>{item.object_id}</strong><span>{item.reasons?.map(knowledgeBlockReasonLabel).join('、')||'存在约束未满足'}</span></div>)}</div>}{result.gaps.length>0&&<div className="query-detail-list">{result.gaps.slice(0,5).map(item=><div key={item.object_id}><strong>{item.object_id}</strong><span>{knowledgeNextActionLabel(item.next_action)}</span></div>)}</div>}<button className="text-action" onClick={()=>setResult(undefined)}>清除本次结果 <X size={14}/></button></>}</section></div>;
}

function KnowledgeDrawer({value,onClose,onDecision,onTask}:{value:GovernedKnowledgeObject;onClose:()=>void;onDecision:(decision:KnowledgeDecision,reason:string)=>Promise<void>;onTask:()=>void}){const [reason,setReason]=useState('');const [busy,setBusy]=useState<KnowledgeDecision>();const canApprove=knowledgeObjectAllows(value,'approve');const canReject=knowledgeObjectAllows(value,'reject');const canReview=canApprove||canReject;const decide=async(decision:KnowledgeDecision)=>{if(!reason.trim()||!knowledgeObjectAllows(value,decision))return;setBusy(decision);try{await onDecision(decision,reason.trim())}finally{setBusy(undefined)}};const taskLabel=value.governance_state==='governed'?'创建更新任务':value.governance_state==='requires_resolution'?'创建处理任务':'创建补料任务';return <Modal title="知识对象" onClose={onClose}><div className="knowledge-drawer-content"><div className="drawer-object-heading"><span className="object-mark is-knowledge"><BookOpen size={16}/></span><div><strong>{value.title}</strong><span><code>{value.id}</code> · v{value.version}</span></div><StatusText value={value.status}/></div><dl className="object-facts"><div><dt>对象类型</dt><dd>{knowledgeObjectTypeLabel(value.object_type)}</dd></div><div><dt>知识层级</dt><dd>{knowledgeLayerLabel(value.layer)}</dd></div><div><dt>证据</dt><dd>{value.evidence_refs.length?value.evidence_refs.join('、'):'缺少来源'}</dd></div><div><dt>影响</dt><dd>{value.impact||'未标记'}</dd></div></dl><section className="drawer-copy"><span>知识陈述</span><p>{value.statement}</p></section><div className={`knowledge-governance-state is-${value.governance_state||'read_only'}`}>{value.governance_state==='governed'?<ShieldCheck size={17}/>:<ShieldAlert size={17}/>}<div><strong>{value.governance_state==='governed'?'已纳入知识治理':canReview?'需要审核决定':'当前版本只读'}</strong><p>{value.governance_message||'当前没有可执行的审核动作，此版本仅供查看。'}</p></div></div>{canReview&&<Field label="审核理由" hint="审核理由会写入知识决定，并生成新的对象版本。"><textarea rows={3} value={reason} onChange={event=>setReason(event.target.value)} placeholder="说明证据核验结果或拒绝原因"/></Field>}<div className="modal-actions"><Button variant="secondary" disabled={Boolean(busy)} onClick={onTask}><ListTodo size={15}/>{taskLabel}</Button>{canReject&&<Button variant="danger" disabled={Boolean(busy)||!reason.trim()} onClick={()=>void decide('reject')}><XCircle size={15}/>{busy==='reject'?'拒绝中…':'拒绝候选'}</Button>}{canApprove&&<Button disabled={Boolean(busy)||!reason.trim()} onClick={()=>void decide('approve')}><Check size={15}/>{busy==='approve'?'批准中…':'批准为知识'}</Button>}</div></div></Modal>}
function PageHeader({eyebrow,title,description,actions}:{eyebrow:string;title:string;description:string;actions?:React.ReactNode}){return <header className="workos-page-header"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></div>{actions&&<div className="workos-header-actions">{actions}</div>}</header>}
function SectionHeader({kicker,title,action}:{kicker:string;title:string;action?:React.ReactNode}){return <header className="workos-section-header"><div><span>{kicker}</span><h2>{title}</h2></div>{action}</header>}
function KnowledgeMetric({label,value,detail,tone='default'}:{label:string;value:string;detail:string;tone?:string}){return <article className={`knowledge-metric is-${tone}`}><span>{label}</span><strong>{value}</strong><small>{detail}</small></article>}
function StatusText({value}:{value:string}){return <span className={`status-text ${statusTone(value)}`}><i/>{knowledgeStatusLabels[value]||statusLabel(value)}</span>}
function statusTone(value:string){if(['approved','verified','valid','active','resolved','published'].includes(value))return'is-success';if(['blocked','conflicted','prohibited','expired','rejected','revoked'].includes(value))return'is-danger';if(['needs_review','candidate','pending','open','source_missing','collecting'].includes(value))return'is-warning';return'is-muted'}
function QueryResultRow({label,value,detail,tone}:{label:string;value:string;detail:string;tone:string}){return <div className="query-result-row"><span className={tone}><i/>{label}</span><strong>{value}</strong><small>{detail}</small></div>}
